import {fireEvent,render,screen} from "@testing-library/react";import {describe,expect,it} from "vitest";import MilitarySalaryUi from "./ui";
describe("MilitarySalaryUi",()=>{it("봉급과 군적금 차이를 입력 전에 설명한다",()=>{render(<MilitarySalaryUi/>);expect(screen.getByText("월급과 군적금은 따로 계산해요")).toBeInTheDocument();expect(screen.getByText(/매월 현금으로 받는 월급이 아니라/)).toBeInTheDocument()});it("적금 가입 선택 시 조건부 입력을 표시한다",()=>{render(<MilitarySalaryUi/>);expect(screen.queryByLabelText("월 납입액")).not.toBeInTheDocument();fireEvent.click(screen.getByRole("radio",{name:"가입"}));expect(screen.getByLabelText("월 납입액")).toBeInTheDocument();expect(screen.getByLabelText(/납입 개월/)).toBeInTheDocument()});it("육군 적금 예시 결과를 표시한다",()=>{render(<MilitarySalaryUi/>);fireEvent.click(screen.getByRole("button",{name:"육군·군적금 예시"}));fireEvent.click(screen.getByRole("button",{name:"예상 월급 계산하기"}));expect(screen.getByText("20,100,000원",{selector:"p"})).toBeInTheDocument();expect(screen.getByRole("heading",{name:"계급별 봉급 내역"})).toBeInTheDocument();expect(screen.getByRole("heading",{name:"군적금 예상 내역"})).toBeInTheDocument()});it("입영일이 없으면 오류를 표시한다",()=>{render(<MilitarySalaryUi/>);fireEvent.click(screen.getByRole("button",{name:"예상 월급 계산하기"}));expect(screen.getByRole("alert")).toHaveTextContent("올바른 입영일")});});
