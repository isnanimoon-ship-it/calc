import type {BmiInput,Sex} from "./types";
export interface RawBmiInput{birthDate:string;referenceDate:string;sex:Sex;heightCm:string;weightKg:string;waistCm:string}
export interface FormError{field:keyof RawBmiInput;message:string}
function validDate(value:string){if(!/^\d{4}-\d{2}-\d{2}$/.test(value))return false;const[y,m,d]=value.split("-").map(Number),date=new Date(Date.UTC(y,m-1,d));return date.getUTCFullYear()===y&&date.getUTCMonth()===m-1&&date.getUTCDate()===d;}
export function validateBmiInput(raw:RawBmiInput):{success:true;data:BmiInput}|{success:false;errors:FormError[]}{const errors:FormError[]=[];if(!validDate(raw.birthDate))errors.push({field:"birthDate",message:"올바른 생년월일을 입력해 주세요."});if(!validDate(raw.referenceDate))errors.push({field:"referenceDate",message:"올바른 기준일을 입력해 주세요."});if(validDate(raw.birthDate)&&validDate(raw.referenceDate)&&raw.birthDate>raw.referenceDate)errors.push({field:"birthDate",message:"생년월일은 기준일보다 늦을 수 없습니다."});
  const height=Number(raw.heightCm),weight=Number(raw.weightKg),waist=raw.waistCm===""?undefined:Number(raw.waistCm);if(!Number.isFinite(height)||height<40||height>250)errors.push({field:"heightCm",message:"키는 40~250cm 범위로 입력해 주세요."});if(!Number.isFinite(weight)||weight<2||weight>500)errors.push({field:"weightKg",message:"체중은 2~500kg 범위로 입력해 주세요."});if(waist!==undefined&&(!Number.isFinite(waist)||waist<40||waist>200))errors.push({field:"waistCm",message:"허리둘레는 40~200cm 범위로 입력해 주세요."});if(errors.length)return{success:false,errors};return{success:true,data:{birthDate:raw.birthDate,referenceDate:raw.referenceDate,sex:raw.sex,heightCm:height,weightKg:weight,...(waist===undefined?{}:{waistCm:waist})}};}
